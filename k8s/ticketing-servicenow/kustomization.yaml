apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml

# secret-template.yaml is intentionally excluded.
#
# Phase 5A must not create even placeholder runtime credentials.
# A real Secret must be handled only in a separately authorized phase.
